{#
UI Element template
type: articles_selection
element fields :
    title
    display
    tags
    limit
    buttonLabel
    buttonUrl
element methods:
    getArticles
#}

{% set articles = ui_element.getArticles(element.tags|default([]), element.limit|default(3), element.typeFilter|default(null)) %}

{{ include('@MonsieurBizSyliusBlogPlugin/admin/ui_element/articles_cards.html.twig') }}
